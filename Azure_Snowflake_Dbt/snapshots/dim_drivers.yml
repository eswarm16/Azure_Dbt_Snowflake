snapshots:
    - name: dim_drivers
      relation: ref('drivers')
      config:
        schema: gold
        database: AZURE_DBT_SNOWFLAKE
        unique_key: DRIVER_ID
        strategy: timestamp
        updated_at: DRIVER_LAST_UPDATED
        dbt_valid_to_current: "to_date('9999-12-31')"