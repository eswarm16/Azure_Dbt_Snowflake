snapshots:
    - name: dim_customers
      relation: ref('customers')
      config:
        schema: gold
        database: AZURE_DBT_SNOWFLAKE
        unique_key: CUSTOMER_ID
        strategy: timestamp
        updated_at: CUSTOMER_LAST_UPDATED
        dbt_valid_to_current: "to_date('9999-12-31')"